/** User requests page — thin wrapper rendering the UserRequestsPanel component. */
import UserRequestsPanel from "@/components/user-requests-panel";

export default function UserRequestsPage() {
  return <UserRequestsPanel />;
}
