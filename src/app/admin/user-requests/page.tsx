/** Admin user-requests page — thin wrapper rendering the AdminRequestsPanel component. */
import AdminRequestsPanel from "@/components/admin-requests-panel";

export default function AdminUserRequestsPage() {
  return <AdminRequestsPanel />;
}
